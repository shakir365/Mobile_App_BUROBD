// To parse this JSON data, do
//
//     final Division = DivisionFromJson(jsonString);

import 'dart:convert';

/*DivisionLeave divisionFromJson(String str) => DivisionLeave.fromJson(json.decode(str));

String divisionToJson(DivisionLeave data) => json.encode(data.toJson());

class DivisionLeave {
  DivisionLeave({
    required this.id,
    required this.name,
    required this.divisionCode,
  });

  String id;
  String name;
  String divisionCode;

  factory DivisionLeave.fromJson(Map<String, dynamic> json) => DivisionLeave(
        id: json["id"],
        name: json["divisionName"],
        divisionCode: json["divisionCode"]
      );

  Map<String, dynamic> toJson() =>
      {"id": id, "divisionName": name, "divisionCode": divisionCode};
}*/
